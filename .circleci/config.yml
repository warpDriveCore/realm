# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#

defaults: &defaults
  docker:
    # specify the version you desire here
    - image: circleci/node:11.4.0-browsers

  working_directory: ~/repo
version: 2
jobs:
  deploy:
    <<: *defaults
    steps:
      - checkout

      # add ssh keys
      - add_ssh_keys:
          fingerprints:
            - "b0:22:b6:a7:59:6e:38:68:7b:8d:76:cf:0e:bf:db:40"
      
      # setup ssh-agent
      - run: which ssh-agent
      - run: eval ssh-agent -s
      - run: mkdir -p ~/.ssh
      - run: echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config

      # prepare remote directories for deployment
      - run: ssh "$DROPLET_CREDENTIALS" "bash -s" < ./tools/deploy.sh
workflows:
  version: 2
  build-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master


